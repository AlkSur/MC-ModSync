pack.local.json
*.local.json
*.local.json.bak
.mcmodsync/
private.key
*.pem
dist/
build/
__pycache__/
*.pyc
client-publish-state.json

# publish-client 两步发布的 dry-run 来源索引缓存（本机中间产物，禁止入库）
.publish-dryrun-cache.json
server-mods/
client-mods/
*.egg-info/
.pytest_cache/

# 凭据文件：计划 [11.2] 未预期 ssh.json（需求方用于存放 SSH 凭证），
# 依 AC-6「凭证零入库」补充覆盖。
ssh.json
/.idea/
/pytest-junit.xml
/pytest-result.txt

# 虚拟环境（原先被逐条写了 2955 行明细，已收敛为两条）
.venv/
venv/

# 脚本中间产物：mods.lock.json 由 gen_lock 按本机客户端 mod 目录生成，
# 含服务器 mod 构成信息，属隐私，禁止入库（2026-09-23 修订）。
mods.lock.json
mods.lock.generated.json

# 杂项
*.log
.DS_Store
Thumbs.db
